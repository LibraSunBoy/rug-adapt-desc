# Implementation Plan: RUG 币工具板块

## Overview

仅修改 `src/views/templatetwo/Tool.vue`，新增 `rugAiTools` 和 `rugBatchTools` 数据，并在模板中追加两个 ToolGrid 子板块。

## Tasks

- [x] 1. 在 Tool.vue 的 data() 中新增工具数据

  - 新增 `rugAiTools` 数组（id 201–206，6 项 AI 单项工具）
  - 新增 `rugBatchTools` 数组（id 301–306，6 项 AI 批量工具）
  - 确保所有 id 与 `freeTools`（1–99）和 `memberTools`（101–199）不冲突
  - _Requirements: 4.1, 4.2, 4.3_

- [x] 2. 在模板中追加 RUG 币工具板块

  - [x] 2.1 在会员工具 ToolGrid 之后添加包裹 div（`margin-top: 12px`）

    - 内含 AI 单项工具 ToolGrid：`title="RUG币工具" tag="AI" :tools="rugAiTools"`，不传 `isMember`
    - 内含 AI 批量工具 ToolGrid：`:tools="rugBatchTools" :isMember="isMember"`
    - 两个 ToolGrid 均绑定 `@tool-click="handleTool"`
    - _Requirements: 1.1, 1.2, 2.1, 2.4, 3.1, 3.2_

  - [ ]\* 2.2 Write property test for isMember 状态一致性

    - **Property 1: isMember 状态与批量工具锁图标一致**
    - **Validates: Requirements 2.2, 2.3**

  - [ ]\* 2.3 Write property test for 工具点击事件路由

    - **Property 2: 工具点击事件路由到 handleTool**
    - **Validates: Requirements 3.1**

  - [ ]\* 2.4 Write property test for 工具项数据结构完整性

    - **Property 3: 工具项数据结构完整性**
    - **Validates: Requirements 4.1, 4.2**

  - [ ]\* 2.5 Write property test for 全局工具 ID 唯一性
    - **Property 4: 全局工具 ID 唯一性**
    - **Validates: Requirements 4.3**

- [x] 3. Checkpoint - 确认渲染正确
  - 确保所有测试通过，如有问题请告知。

## Notes

- 标有 `*` 的子任务为可选测试任务，可跳过以加快 MVP 交付
- 所有改动仅限 `Tool.vue` 一个文件
